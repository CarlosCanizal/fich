table
  tr
    th
      |Nombre
    th
      |Descripcion
    th
      |Precio
    th
      |Accciones
  tr(data-ng-repeat="item in services.list")
    td
      |{{item.name}}
    td
      |{{item.description}}
    td
      |{{item.price}}
    td
      a( ui-sref="service({objectId:item.objectId})" )
        |editar